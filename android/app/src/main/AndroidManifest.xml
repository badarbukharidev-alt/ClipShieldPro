<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROCESSING" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Required on Android 13+ to show the render progress notification. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Without a battery-optimisation exemption, aggressive OEM power managers
         (Xiaomi, Oppo, Vivo, Samsung) kill the render process the moment the app
         leaves the screen, even though a foreground service is running. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!--
        Lets the app hand a downloaded update to the system installer, so a
        release can be applied without leaving for a browser. It does not
        install anything silently: Android still shows its own confirmation,
        and from Android 8 the user must additionally allow this app under
        "Install unknown apps". The APK is signature-checked against the
        running build first; see ApkInstaller.
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:label="ClipShield Pro"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true"
        android:hardwareAccelerated="true"
        android:largeHeap="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <!--
            Foreground Service for long-running video rendering.

            BOTH types are declared, and that is not belt and braces.
            "mediaProcessing" only exists from API 35 (Android 15). On Android 14
            the system does not recognise it, so the service resolved to NO type
            at all, and an app targeting API 34+ that calls startForeground()
            without a valid type is killed on the spot with
            MissingForegroundServiceTypeException.

            That is exactly what happened: the app vanished the instant Render
            was tapped, on every Android 14 phone, and the next launch reported
            "Render was interrupted before it finished".

            "dataSync" has existed since API 29, so Android 10-14 use that and
            Android 15+ can use the more accurate one. The plugin calls the
            two-argument startForeground(), so the type comes from here.
        -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="dataSync|mediaProcessing"
            android:stopWithTask="false"
            android:exported="false" />

        <!--
            Hands a rendered file to another app as a content:// URI. Required
            for sharing straight into WhatsApp, Instagram, YouTube and the rest:
            a file:// URI has thrown FileUriExposedException since Android 7.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
        <package android:name="com.instagram.android" />
        <package android:name="com.google.android.youtube" />
        <package android:name="com.facebook.katana" />
        <package android:name="com.zhiliaoapp.musically" />
        <package android:name="com.ss.android.ugc.trill" />
        <package android:name="org.telegram.messenger" />
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="video/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="image/*" />
        </intent>
    </queries>
</manifest>
